<h1>id: <%= p@task.id %>のタスク編集ページ</h1>

<%= form_with(model: @task, local:true) do |f| %>
  <%= f.label :content,'メッセージ'%>
  <%= f.text_field :content %>
  
  <%= f.submit '登録'%>
<% end %>

<%= link_to '戻る', @task %>